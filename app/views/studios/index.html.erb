<h1>Studios</h1>

<% @studios.each do |studio| %>
  <section id="studio-<%= studio.id %>">
    <h2><%= studio.name %></h2>
    <p><%= studio.location %></p>
    <p>Movies:</p>
    <% studio.movies.each do |movie| %>
      <%= movie.title %>
    <% end %>
  </section>
<% end %>